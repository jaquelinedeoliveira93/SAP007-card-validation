
/**
 function validator(numeroCartao) {
    for (let i = 0; i < numeroCartao.length; i++){
        let Digitos = numeroCartao[i];
        let converteDigitos = Digitos.toString();
        Digitos.revarse();

        
          if (o numero for impar não faça nada)
          if ((digitosDoCartao *= 2) > 9)
          digitosDoCartao -= 9
         
        soma += digitosDoCartao,

        return (soma % 10) === 0;
        

         
};


    if (luhn (numeroCartao)){
        alert("Cartão de crédito Valido");
    }else{
        alert("Cartão de crédito Invalido");
    } 
}

export default validator;
*/